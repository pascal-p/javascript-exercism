/*
  
 */

export const encode = (vnum) => {
  // throw new Error('Remove this statement and implement this function');

  if (vnum.length === 1 && vnum[0] <= 0x7f) return vnum
};

export const decode = () => {
  throw new Error('Remove this statement and implement this function');
};
